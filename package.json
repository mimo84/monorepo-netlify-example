{
  "private": true,
  "workspaces": [
    "website",
    "packages/*"
  ],
  "scripts": {
    "build:website": "yarn workspace website build",
    "build:my-library": "yarn workspace my-library build"
  }
}
